<%# @wines.each do |wine| %>
  <%# if wine.designation != nil && wine.winery != nil  %>
    <!-- <h2><%#= wine.designation %></h2>
    <p><%#= wine.winery %></p> -->
  <%# end %>
<%# end %>


  <% @wines.each do |wine| %>
      <div class="wine-card">
        <h1><%= wine.winery %></h1>
        <h2><%= wine.designation %></h2>
        <% link_to 'ckeck it out!', "/wines/#{wine.id}" %>
      </div>
  <% end %>
